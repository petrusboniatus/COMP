#include <stdio.h>
#include "test/IntegrationTest.h"

/*
 * # Responsibilities:
 * 1. For know it only runs the test, so it's easy to compile and make the correction
 */


int main() {
	doIntegrationTest();
	return 0;
}
